var app = app || {};

(function() {
    'use strict';

    app.const = {
        // applciation specific html id attr. values
        ID: {
            todoList: '#todo-list'
        },
        // Keyboard actions.
        KEYS: {
            ENTER_KEY: 13
        }
    };

})();
